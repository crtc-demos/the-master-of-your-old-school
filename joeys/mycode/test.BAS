*LOAD L.LOGS
*LOAD L.SIN

*LOAD O.SIERPA
*LOAD O.ROTA
*LOAD O.PHASE
*LOAD O.WHIRL

; MODE 7

; sierpa
; CALL &27E8
; TIME=0 : REPEAT UNTIL TIME>=2000
; CALL &282D

; rota
CALL &3656
TIME=0 : REPEAT UNTIL TIME>=2000
CALL &36B3

; phase
CALL &1B00
TIME=0 : REPEAT UNTIL TIME>=2000
CALL &1B4C

; whirl
CALL &5106
TIME=0 : REPEAT UNTIL TIME>=2000
CALL &514A

